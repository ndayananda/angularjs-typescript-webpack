import * as angular from 'angular';
import { appConstants } from './../../core/constants';

export class MainLayoutController {
    constructor(
        private appConstants: object
    ) {
        'ngInject';
    }

    $onInit() {}
}